apiVersion: v1
kind: Service
metadata:
  name: kube-registry
  namespace: kube-system
spec:
  # clusterIP: 10.96.0.99
  ports:
  - name: https
    port: 5000
    protocol: TCP
    targetPort: 5000
  sessionAffinity: None
  type: ClusterIP
  selector:
    k8s-app: kube-registry
